# Reply 15 — yes to the crafting swap, narrower than FilterBlade's. And four confirmations.

Taking §1 first because it is the only thing that needs me. Everything else is short.

Both retractions accepted with thanks — and the second one is worth more than a retraction.
*"T4 puts the accent in the text"* is `_muted_is_authored` being load-bearing in a way I asserted
but had not seen proved on real output. Three categories' T4 rows reading `#c39691`, `#b4827d`,
`#a5787d` against the same grey is the design working. That closes the depth-2 question for good.

---

## ★ 1. Yes — and the scope is the answer, not the colour

**Take the swap. Same channel, same colour, same `120 235 210`.**

The measurement decides it. 115 of 282 bases where the crafting block draws *byte-identical* to the
plain-rare block is not a near-miss — it is `gen_order −10` buying first look at an item and then
spending it on nothing. And the other 167 are worse in a specific way: one rung louder reads as
*"this base ranks higher"*, which is a claim about the base type. The thing we need to say is about
*this instance*. The ladder has no way to say that, because ranking is all a ladder does.

You are also right that this is not a new axis, and the reason is the one you found: **`replica` is
jewels' solid exactly, `foulborn` is breach's.** Swaps already reuse a hue in a different channel.
A 27th accent would be inventing vocabulary for something the vocabulary already covers.

On you and FilterBlade landing on the same colour independently: that is the useful part of your
report. `0 240 190` appearing across `rareid`, `exoticmods`, `magicid`, `memorystrand`,
`jewels->abyss` — 24 purposes, every family — means they are not colouring *crafting*. They are
colouring **"this instance beats its rarity"**, which is the sentence in my own presets. Neither of
us reached for a category hue. Two people solving it the same way is worth more than either
argument.

### Where I diverge: the swap fires on T2–T3 only

FilterBlade scopes it to the whole purpose. I am scoping it to **the two rungs where the plate is
neutral**, and that difference is the whole answer to your ⚠️.

> **A text swap only fires where the plate is neutral. Never on T0 or T1.**
> Those rungs are house-fixed idioms — red on white, white on red — whose own colours already say
> "exceptional" louder than any text could. A swap there overwrites a louder statement with a
> quieter one.

It is in the presets as `_swaps_never_paint_the_house_rungs` and governs all four swaps.

**So the cost you stated plainly gets smaller, and lands in the right place.** Crafting gives up
rarity-through at **T2 and T3 only**. The four ilvl bands sit at T4/T5 and keep it — which matters,
because at the floor rarity is the *only* thing distinguishing an i84 base from another i84 base.
There is nothing exceptional to claim there; the block is bulk by volume, as my own override note
says. Taking the text there would spend the channel to say "this is ordinary".

That is also why I would not adopt their version wholesale: paying the cost across a whole purpose
buys nothing at the rungs where the block has no claim to make.

### And yes — Perfect Defence opens at T0

You are right that the author's instinct, FilterBlade's shape and my own prose are the same
arrangement, and I did not notice I had written T0's definition into a T2 note. *"A perfect roll is
a reason to stop moving"* is drop-everything. Their `exoticbases` spending red-on-white on exactly
one base is the same restraint I was claiming to protect by refusing it.

```
Equipment/Crafting Priority.json (10)
was   T2 T2 T3 T3 T4 T4 T5 T5 T5 T5
now   T0 T2 T3 T3 T4 T4 T5 T5 T5 T5
```

**Only the top row moves.** Over Quality stays at T2 — it needs a rung where the swap can fire, and
under the new rule T1 is not one. A 99th-percentile i84 base is rarer than a Q21+ base and should
not share its rung anyway; they were only paired because the old override opened at T2 and had
nowhere else to put them.

`_q21_scope` narrows from T0–T2 to **T2**. In practice nothing moves: Over Quality is the block it
was written for, and that is where it sits.

Build order is yours, but the swap and the crafting answer are one build as you say, and the T0
change is a single value in the override.

## 2. Magic Net — `T3 T5` confirmed, annotation corrected to `(2)`

Corrected in the kit. Your reading of my own `_rule` is right and I annotated it wrong.

I am **keeping the override** even though it now agrees with the template, which is worth a word
since normally I would delete a redundant one. Its top rung is an argument — *"a declutter net's
best tier is not 'worth picking up', it is 'not yet worth hiding'"* — and the gear template would
still open it at T2 if the depth ever changed. Deleting it would leave the reasoning nowhere and
invite exactly that. It is the one override I want kept for its note.

## 3. `painted_t3_needs_t2` is **per theme category**

Your reading. The invariant is a claim about *recognition* — can a player name this family? — and
the player sees the merged category, not the file it came from. `rung_by_depth` is scoped per
ladder because **emission** is per ladder; this check is not emission.

So Base Jewels at `T3` alone is fine, because Jewels as a category has its T2 in Abyss and Cluster.
Scope is written into the invariant now so the validator has no choice to make.

## 4. The four depth-1 warnings — all confirmed, silence them

Allowlisted in the map with a reason each. Three Allflame files are 3.29 league drops, and T2 is
what new content wants: you do not yet know what it is worth, so *"worth picking up"* is the honest
rung. Heist Targets are the smuggler's caches — the thing the contract was *for* — and heist never
reaches the bulk rungs anyway.

**Do not widen the allowlist** to the category or to depth-1 generally. It caught Legacy and
Chancing and it will catch the next one. A warning nobody can clear is a warning everybody stops
reading, which is the only reason I am confirming these individually rather than relaxing the check.

## 5. `theme-standard.md` — fixed, and thank you for not trusting it

All eight T6 lines corrected, the shape list updated to ten assigned (only UpsideDownHouse held),
and the currency table now ends at T4 = scrolls to match the override. There is a banner at the top
saying the machine files win any disagreement.

That file went stale because it is prose and the JSON is not. Your instinct to follow the machine
files is the correct default permanently, not just this round.

## 6. On the circular sort

Worth naming what that was, because it is the most interesting bug in the thread: **the rung a tier
got depended on the theme it already had.** It was stable exactly as long as nothing changed, and it
failed precisely on the ladders the reshape touched — so the reshape looked like the cause. And the
author's report of it was *"crafting bases, seems no theme applied yet"*, which was true in the way
that matters and false in the way that would have sent you looking in the wrong place. Applied,
inverted, flat grey.

---

## What I am taking from your caveat

**"Green is not evidence."** Agreed, and I would rather you send what the load turns up than a
clean run. Both new accents in this drop — `205 40 95` and `220 238 242` — have never been seen on
a real drop; they were chosen against measured neighbours, which is not the same as being looked
at in a map at night. `205 40 95` in particular sits 11° from ritual, and 11° is a number I trust
on a swatch board and not yet on the ground.

The border ratio moving to **17 of 422, states composing on 405**, is the number I keep watching.
It moved with the tree and the ratio held through two accent splits.

Still mine: the icon-floor sweep.
