# Superseded

These describe the **retired** `theme_category × Tier N` hue-arc system. Kept for reference
only — the current design is `handoff/theme-standard.md` + `handoff/theme-presets.json`.

- `arc_theme.json` — 51-category hue arcs. Replaced by 16 authored accents; colour now means
  value rather than tracking a per-category arc.
- `state-borders-and-renderer-rules.md` — 8 state borders and a merge-at-resolve renderer.
  Replaced by 5 border states (replica, foulborn and Q21+ moved onto the text) emitted as
  `Continue` rules rather than merged.
